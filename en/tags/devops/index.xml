<rss xmlns:atom="http://www.w3.org/2005/Atom" version="2.0"><channel><title>devops - Tag - Coding Life Share</title><link>https://blog.xwi88.com/en/tags/devops/</link><description>devops - Tag - Coding Life Share</description><generator>Hugo -- gohugo.io</generator><language>en</language><managingEditor>278810732@qq.com (xwi88)</managingEditor><webMaster>278810732@qq.com (xwi88)</webMaster><copyright>This work is licensed under a Creative Commons Attribution-NonCommercial 4.0 International License.</copyright><lastBuildDate>Sat, 26 Feb 2022 23:51:01 +0800</lastBuildDate><atom:link href="https://blog.xwi88.com/en/tags/devops/" rel="self" type="application/rss+xml"/><item><title>Solving Painfully Slow git clone</title><link>https://blog.xwi88.com/en/git-clone-slowly-solve/</link><pubDate>Sat, 26 Feb 2022 23:51:01 +0800</pubDate><author>xwi88</author><guid>https://blog.xwi88.com/en/git-clone-slowly-solve/</guid><description><![CDATA[<p>If you work with GitHub a lot, operations like <code>clone</code>, <code>fetch</code>, <code>pull</code> and <code>push</code> often fail with mysterious timeouts or crawl along painfully slowly — a real drain on productivity. This post walks through several working solutions.</p>
<p>There are many possible causes; the most likely is that your network is being throttled — for example at <code>github.global.ssl.fastly.net</code>.</p>
<blockquote>
<p>Using the <a href="https://github.com/v8fg/docker-compose-resources.git" target="_blank" rel="noopener noreffer ">docker-compose-resources</a> project as an example: <code>git clone https://github.com/v8fg/docker-compose-resources.git</code></p>
</blockquote>
<h2 id="solutions">
    <a href="#solutions" class="header-anchor" aria-label="Link to Solutions"></a>Solutions</h2>
<h3 id="github-mirror-repositories">
    <a href="#github-mirror-repositories" class="header-anchor" aria-label="Link to GitHub mirror repositories"></a>GitHub mirror repositories</h3>
<ul>
<li><strong>Search the web for a mirror URL, swap it in, then download</strong>
<ul>
<li><code>https://hub.fastgit.org</code> (availability not guaranteed)</li>
<li><code>https://github.91chi.fun</code> (availability not guaranteed)</li>
</ul>
</li>
<li>Install a browser extension: <strong>GitHub accelerator</strong>, then clone via the accelerated URL it provides</li>
<li><em>Clone through another Git hosting mirror</em></li>
<li><em>Download the ZIP directly in the browser</em></li>
</ul>
<h3 id="proxy-acceleration">
    <a href="#proxy-acceleration" class="header-anchor" aria-label="Link to Proxy acceleration"></a>Proxy acceleration</h3>
<ul>
<li><strong>proxy</strong> <em>not really recommended</em></li>
<li><a href="https://github.com/hunshcn/gh-proxy" target="_blank" rel="noopener noreffer ">gh-proxy</a> (<strong>recommended</strong>)</li>
<li><a href="https://github.com/creazyboyone/FastGithub" target="_blank" rel="noopener noreffer ">FastGithub</a> (<strong>recommended ☆☆☆☆☆☆</strong>)</li>
</ul>
<h4 id="fastgithub">
    <a href="#fastgithub" class="header-anchor" aria-label="Link to FastGithub"></a>FastGithub</h4>
<blockquote>
<p>A miracle tool for GitHub acceleration: fixes GitHub failing to open, avatars not loading, releases failing to upload/download, and git-clone / git-pull / git-push errors.</p>
</blockquote>
<h5 id="features">
    <a href="#features" class="header-anchor" aria-label="Link to Features"></a>Features</h5>
<ul>
<li>Clean IP resolution for the relevant domains</li>
<li>IP speed testing, automatically picking the fastest IP</li>
<li>Custom TLS connection configuration per domain</li>
<li>Replaces Google CDN resources, fixing the failure to load JS and CSS on many overseas sites</li>
</ul>
<h5 id="install--configure">
    <a href="#install--configure" class="header-anchor" aria-label="Link to Install &amp; configure"></a>Install &amp; configure</h5>
<blockquote>
<ul>
<li><a href="https://github.com/creazyboyone/FastGithub" target="_blank" rel="noopener noreffer ">FastGithub</a></li>
<li><a href="https://github.com/creazyboyone/FastGithub/releases" target="_blank" rel="noopener noreffer ">github-release</a></li>
</ul>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span><span class="lnt">14
</span><span class="lnt">15
</span><span class="lnt">16
</span><span class="lnt">17
</span><span class="lnt">18
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># linux-x64</span>
</span></span><span class="line"><span class="cl"><span class="c1">## terminal start</span>
</span></span><span class="line"><span class="cl">sudo ./fastgithub
</span></span><span class="line"><span class="cl"><span class="c1"># set the system auto-proxy to http://127.0.0.1:38457, or manually proxy http/https to 127.0.0.1:38457</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">## service start</span>
</span></span><span class="line"><span class="cl">sudo ./fastgithub start // install and start as a systemd service
</span></span><span class="line"><span class="cl">sudo ./fastgithub stop  // uninstall and remove the systemd service
</span></span><span class="line"><span class="cl"><span class="c1"># set the system auto-proxy to http://127.0.0.1:38457, or manually proxy http/https to 127.0.0.1:38457</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># macOS-x64</span>
</span></span><span class="line"><span class="cl"><span class="c1">## 1. start fastgithub</span>
</span></span><span class="line"><span class="cl"><span class="c1">## 2. install cacert/fastgithub.cer and set it as trusted</span>
</span></span><span class="line"><span class="cl"><span class="c1"># set the system auto-proxy to http://127.0.0.1:38457, or manually proxy http/https to 127.0.0.1:38457</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># certificate verification</span>
</span></span><span class="line"><span class="cl"><span class="c1"># if git reports &#34;SSL certificate problem&#34;,</span>
</span></span><span class="line"><span class="cl"><span class="c1"># disable git&#39;s certificate verification: git config --global http.sslverify false</span>
</span></span></code></pre></td></tr></table>
</div>
</div><h5 id="security--legality-note">
    <a href="#security--legality-note" class="header-anchor" aria-label="Link to Security &amp; legality note"></a>Security &amp; legality note</h5>
<blockquote>
<p>Excerpted from <a href="https://github.com/creazyboyone/FastGithub#readme" target="_blank" rel="noopener noreffer ">FastGithub.README</a></p>
</blockquote>
<p><em>FastGithub generates a self-signed CA certificate for each host, stored under the cacert folder. Client devices must install and unconditionally trust this self-signed CA certificate. Do not leak the certificate&rsquo;s private key to anyone, to avoid losses.</em></p>
<p><em>&ldquo;FastGithub&rsquo;s local proxy only uses the international exit channels provided by the public telecom network. The traffic between overseas GitHub servers and the FastGithub program on a domestic user&rsquo;s machine flows over normal channels, with no extra encryption applied beyond the page&rsquo;s own TLS (unlike the encrypted traffic of a proxy). The entire proxy process after FastGithub retrieves the page data takes place domestically, so the regulations on international internetworking do not apply.&rdquo;</em></p>
<h3 id="hosts-tweak">
    <a href="#hosts-tweak" class="header-anchor" aria-label="Link to Hosts tweak"></a>Hosts tweak</h3>
<blockquote>
<p>The idea is to use <code>nslookup</code> or <code>dig</code> to find a reachable IP for the domain, then add an <code>ip -&gt; domain</code> mapping to your hosts file and flush the DNS cache.</p>
</blockquote>
<p>The hosts file is what an OS uses to quickly resolve IP addresses and hostnames. On Linux it is stored in ASCII under <code>/etc</code> and named <code>hosts</code> (on some distros the path differs — for example Debian also has <code>/etc/hostname</code>). It holds IP-to-hostname mappings and host aliases. When no DNS server is available, every network program on the system queries this file to resolve a hostname to an IP; otherwise DNS is used. Mapping frequently visited domains to their IPs in hosts is a handy way to speed up access.</p>
<blockquote>
<ul>
<li><code>nslookup github.global.ssl.fastly.net</code></li>
<li><code>nslookup github.com</code></li>
</ul>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># vi /etc/hosts</span>
</span></span><span class="line"><span class="cl">xxx github.global.ssl.fastly.net
</span></span><span class="line"><span class="cl">xx github.com
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># refresh DNS cache</span>
</span></span><span class="line"><span class="cl"><span class="c1"># linux</span>
</span></span><span class="line"><span class="cl">sudo /etc/init.d/networking restart
</span></span><span class="line"><span class="cl"><span class="c1"># or nscd -i hosts</span>
</span></span><span class="line"><span class="cl"><span class="c1"># arch linux</span>
</span></span><span class="line"><span class="cl">/etc/rc.d/nscd restart
</span></span><span class="line"><span class="cl"><span class="c1">## macos</span>
</span></span><span class="line"><span class="cl">lookupd -flushcache
</span></span><span class="line"><span class="cl"><span class="c1"># dscacheutil -flushcache</span>
</span></span></code></pre></td></tr></table>
</div>
</div>]]></description></item></channel></rss>